import React from "react";
import { NativeBaseProvider} from "native-base";

function App() {
  return (
    <NativeBaseProvider>
      <button>Olá</button>
    </NativeBaseProvider>
  );
}

export default App;
